import { HeroAccordion } from "./HeroAccordion";

/**
 * Ana sayfa hero'su — accordion düzeni (varyant 1) sabitlendi.
 * Varyant seçici ve diğer üç düzen (Showcase, Bento, Liste) kaldırıldı;
 * bileşenleri repo'da duruyor, geçmişten geri alınabilir (cc2b1ea).
 */
export function Hero() {
  return <HeroAccordion />;
}
